# توثيق منصة ClassWithOnline التعليمية

## 📋 جدول المحتويات
1. [نظرة عامة](#نظرة-عامة)
2. [المتطلبات التقنية](#المتطلبات-التقنية)
3. [التثبيت والإعداد](#التثبيت-والإعداد)
4. [البنية الأساسية](#البنية-الأساسية)
5. [الميزات الرئيسية](#الميزات-الرئيسية)
6. [دليل المستخدم](#دليل-المستخدم)
7. [دليل المطور](#دليل-المطور)
8. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## نظرة عامة

**منصة ClassWithOnline** هي منصة تعليمية إلكترونية متكاملة تقدم دورات تعليمية في اللغة الإنجليزية وشروحات الصف الثالث الثانوي. تجمع المنصة بين تقنيات الويب الحديثة والتصميم سهل الاستخدام لتوفير تجربة تعليمية شاملة.

### الأهداف الرئيسية
- توفير محتوى تعليمي عالي الجودة
- تسهيل التواصل بين الطلاب والمعلمين
- توفير أدوات تقييم تفاعلية
- إدارة فعالة للمحتوى التعليمي

---

## المتطلبات التقنية

### للتشغيل المحلي
- **PHP**: إصدار 7.4 أو أحدث
- **متصفح ويب**: Chrome, Firefox, Safari, Edge (الإصدارات الحديثة)
- **اتصال إنترنت**: مطلوب للاتصال بـ Firebase

### الخدمات الخارجية
- **Firebase Realtime Database**: لتخزين البيانات
- **Firebase Authentication**: للتحقق من المستخدمين
- **Firebase Storage**: لتخزين الملفات (اختياري)

---

## التثبيت والإعداد

### الخطوة 1: تحميل المشروع
```bash
git clone <repository-url>
cd project_edu
```

### الخطوة 2: بدء خادم PHP
```bash
php -S localhost:8000
```

### الخطوة 3: الوصول للموقع
افتح المتصفح وانتقل إلى:
```
http://localhost:8000
```

### الخطوة 4: إعدادات Firebase
تأكد من أن إعدادات Firebase في `assets/js/firebase-app.js` صحيحة:
```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_AUTH_DOMAIN",
    databaseURL: "YOUR_DATABASE_URL",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_STORAGE_BUCKET",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID",
    measurementId: "YOUR_MEASUREMENT_ID"
};
```

---

## البنية الأساسية

### هيكل المشروع
```
project_edu/
├── index.html                    # الصفحة الرئيسية
├── README.md                     # ملف التعليمات
├── DOCUMENTATION.md              # هذا الملف
│
├── admin/                        # لوحة التحكم
│   ├── index.html               # لوحة التحكم الرئيسية
│   ├── courses-advanced.html    # إدارة الدورات المتقدمة
│   ├── interactive-features.html # الميزات التفاعلية
│   ├── manage-courses.php       # API لإدارة المحتوى
│   └── upload-handler.php       # معالج تحميل الملفات
│
├── assets/                       # الموارد الثابتة
│   ├── css/
│   │   └── style.css            # أنماط الموقع الرئيسية
│   └── js/
│       ├── firebase-app.js      # وظائف Firebase الأساسية
│       └── admin-functions.js   # وظائف الإدارة المتقدمة
│
├── includes/                     # ملفات PHP المساعدة
│   └── firebase-config.php      # إعدادات Firebase للـ PHP
│
└── uploads/                      # مجلد الملفات المحملة
```

---

## الميزات الرئيسية

### 1. الأقسام الثلاثة
| القسم | الوصف | المستهدفون |
|------|-------|-----------|
| **STEP** | برنامج تأسيس اللغة الإنجليزية | المبتدئون |
| **الثانوي** | شروحات الصف الثالث الثانوي | طلاب الثانوية |
| **الإنجليزية** | برنامج شامل بمستويات متعددة | جميع المستويات |

### 2. المحتوى التعليمي
- **الفيديوهات**: شروح فيديو تفصيلية من YouTube أو مصادر أخرى
- **ملفات PDF**: كتب وملفات تعليمية قابلة للتحميل
- **أوراق العمل**: أوراق عمل محلولة وغير محلولة

### 3. الاختبارات المحاكاة
- اختبارات تفاعلية متعددة الخيارات
- تقييم فوري للإجابات
- حفظ النتائج والإحصائيات
- مستويات صعوبة مختلفة

### 4. نظام الرسائل والتنبيهات
- نظام رسائل لتواصل الطلاب
- تنبيهات فورية للإعلانات المهمة
- أولويات مختلفة للتنبيهات

### 5. لوحة التحكم
- إدارة شاملة للدورات
- إضافة وتعديل وحذف المحتوى
- إحصائيات وتحليلات
- تصدير البيانات

---

## دليل المستخدم

### للطلاب

#### الوصول للموقع
1. افتح المتصفح وانتقل إلى `http://localhost:8000`
2. ستجد الصفحة الرئيسية مع جميع الأقسام

#### استكشاف الدورات
1. اختر أحد الأقسام الثلاثة من القائمة الجانبية
2. اضغط على "عرض الدورة" لمشاهدة تفاصيل الدورة
3. شاهد الفيديوهات وحمّل الملفات

#### إجراء الاختبارات
1. انتقل إلى قسم "الاختبارات المحاكاة"
2. اختر الاختبار الذي تريد
3. أجب على جميع الأسئلة
4. اضغط "إرسال الإجابات" للحصول على النتيجة

#### التواصل
1. انتقل إلى قسم "الرسائل والتنبيهات"
2. اكتب رسالتك وأرسلها
3. تابع الرسائل الأخرى والتنبيهات المهمة

### للمعلمين والمسؤولين

#### الدخول لوحة التحكم
1. انتقل إلى `http://localhost:8000/admin/`
2. ستجد لوحة التحكم الرئيسية

#### إدارة الدورات
1. اذهب إلى `admin/courses-advanced.html`
2. أضف دورة جديدة بملء النموذج
3. أضف فيديوهات وملفات للدورة
4. عدّل أو احذف الدورات الموجودة

#### إنشاء الاختبارات
1. اذهب إلى `admin/interactive-features.html`
2. املأ بيانات الاختبار
3. أضف الأسئلة والخيارات
4. حدد الإجابة الصحيحة لكل سؤال

#### إرسال التنبيهات
1. في لوحة التحكم، اذهب إلى قسم "التنبيهات"
2. اكتب عنوان ونص التنبيه
3. اختر نوع وأولوية التنبيه
4. اضغط "إرسال التنبيه"

#### مراقبة الإحصائيات
1. اذهب إلى قسم "الإحصائيات"
2. شاهد عدد الدورات والاختبارات والرسائل
3. عرض تفاصيل نتائج الاختبارات
4. صدّر البيانات إذا لزم الأمر

---

## دليل المطور

### وظائف Firebase الأساسية

#### قراءة البيانات
```javascript
readFromFirebase('courses', (data) => {
    console.log('Courses:', data);
});
```

#### كتابة البيانات
```javascript
writeToFirebase('courses/course1', {
    title: 'Course Title',
    description: 'Course Description'
});
```

#### إضافة بيانات جديدة
```javascript
addToFirebase('courses', {
    title: 'New Course',
    description: 'Description'
}).then((courseId) => {
    console.log('Course added with ID:', courseId);
});
```

#### تحديث البيانات
```javascript
updateFirebase('courses/course1', {
    title: 'Updated Title'
});
```

#### حذف البيانات
```javascript
deleteFromFirebase('courses/course1');
```

### وظائف متقدمة

#### تحميل الملفات
```javascript
uploadFile(file, 'pdf', (data) => {
    console.log('File uploaded:', data.file_url);
});
```

#### البحث عن المحتوى
```javascript
searchContent('keyword', (results) => {
    console.log('Search results:', results);
});
```

#### تصدير البيانات
```javascript
exportToCSV(data, 'export.csv');
```

### API Endpoints

#### الدورات
```
GET  /admin/manage-courses.php?action=get_courses
GET  /admin/manage-courses.php?action=get_course&id=courseId
POST /admin/manage-courses.php?action=add_course
POST /admin/manage-courses.php?action=update_course
POST /admin/manage-courses.php?action=delete_course
```

#### الاختبارات
```
GET  /admin/manage-courses.php?action=get_quizzes
POST /admin/manage-courses.php?action=add_quiz
```

#### الرسائل
```
GET  /admin/manage-courses.php?action=get_messages
POST /admin/manage-courses.php?action=add_message
```

#### التنبيهات
```
GET  /admin/manage-courses.php?action=get_notifications
POST /admin/manage-courses.php?action=add_notification
```

### التخصيص

#### تغيير الألوان
عدّل متغيرات CSS في `assets/css/style.css`:
```css
:root {
    --primary-color: #2c3e50;
    --secondary-color: #3498db;
    --success-color: #27ae60;
    --danger-color: #e74c3c;
    --warning-color: #f39c12;
}
```

#### إضافة ميزات جديدة
1. أضف الدوال في `assets/js/firebase-app.js`
2. أضف الواجهات في `index.html` أو `admin/` pages
3. أضف الأنماط في `assets/css/style.css`

---

## استكشاف الأخطاء

### المشكلة: الموقع لا يحمل البيانات من Firebase

**الحل:**
1. تحقق من إعدادات Firebase في `assets/js/firebase-app.js`
2. تأكد من وجود اتصال إنترنت
3. افتح console المتصفح (F12) وتحقق من الأخطاء
4. تأكد من قواعد أمان Firebase

### المشكلة: الملفات لا تُحمّل بنجاح

**الحل:**
1. تأكد من وجود مجلد `uploads/`
2. تحقق من أذونات المجلد (755)
3. تأكد من أن حجم الملف أقل من 50 MB
4. تحقق من نوع الملف المسموح

### المشكلة: الاختبارات لا تحفظ النتائج

**الحل:**
1. تأكد من اتصال Firebase
2. تحقق من قواعد أمان Firebase للكتابة
3. افتح console المتصفح للتحقق من الأخطاء

### المشكلة: الرسائل والتنبيهات لا تظهر

**الحل:**
1. تحقق من أن البيانات موجودة في Firebase
2. تأكد من تحديث الصفحة (F5)
3. تحقق من أن JavaScript مفعّل في المتصفح

---

## نصائح الأداء

### تحسين السرعة
1. استخدم CDN للملفات الثابتة
2. ضغط الصور قبل التحميل
3. استخدم lazy loading للفيديوهات
4. قلل عدد استدعاءات Firebase

### تحسين الأمان
1. استخدم HTTPS في الإنتاج
2. فعّل قواعد أمان Firebase
3. تحقق من صحة المدخلات على الخادم
4. استخدم متغيرات البيئة للمفاتيح السرية

---

## الدعم والمساعدة

للحصول على الدعم:
1. راجع هذه التوثيق
2. تحقق من console المتصفح للأخطاء
3. اتصل بفريق التطوير
4. راجع توثيق Firebase الرسمية

---

## الترخيص

جميع الحقوق محفوظة © 2024 ClassWithOnline

---

**آخر تحديث:** ديسمبر 2024
